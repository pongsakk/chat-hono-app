{
  "name": "chat-hono-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "start": "bun run index.ts",
    "build": "bun build index.ts --outdir ./dist --target bun"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "mongodb": "^7.1.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
